下面把路线图进一步落到“开发者应该如何实现”的层面。

最核心的原则是：

> **不要一开始就重写 Minecraft 的整个区块依赖系统。保留原版 `ChunkPyramid`、`ChunkDependencies` 和 `GenerationChunkHolder` 作为正确性的来源，只接管任务的准入、优先级、执行资源和完成节奏。**

Minecraft 1.21 已经把区块生成拆分到 `ChunkGenerationTask`、`GeneratingChunkMap`、`GenerationChunkHolder` 和 `net.minecraft.world.level.chunk.status` 中，并由 `ChunkPyramid` 描述 ProtoChunk 到 FullChunk 的步骤与邻区块依赖。这实际上为外部调度层提供了比旧版本更清晰的介入边界。([NeoForged Documentation][1])

> 注意：公开 Javadoc 页面显示的是 1.21.0 早期 NeoForge 构建。正式编码时必须以项目使用的准确 1.21.1 NeoForge 反编译源码为准，下面的方法名用于确定架构位置。

---

# 一、总体实现架构

建议将系统分成四层：

```text
原版需求层
Chunk Ticket / GenerationChunkHolder / ChunkPyramid
                ↓
SteadyChunks 调度层
优先级、背压、取消、阶段令牌、公平性
                ↓
执行层
固定工作线程池 + 原版主线程邮箱 + 光照/I/O执行器
                ↓
阶段实现层
原版 / FastNoise / Bye-Pregen / 结构模组
```

SteadyChunks 不应该直接实现一个新的 `ChunkGenerator`，也不应该让自己的调度器理解每一种地形算法。

它应该做的是：

1. 原版判断某个区块需要执行哪个 `ChunkStep`；
2. 原版准备好邻区块依赖；
3. SteadyChunks 决定这个步骤现在是否允许开始；
4. 允许后调用原来的阶段函数；
5. 在 Future 完成时释放资源并记录结果；
6. 决定后续任务何时继续。

`ChunkMap` 当前公开了 `applyStep`、`scheduleGenerationTask` 和 `runGenerationTasks` 等入口；`ChunkGenerationTask` 则包含 `runUntilWait`、分层调度和取消标记，因此第一版应围绕这些位置建立包装，而不是覆盖整个区块系统。([nekoyue.github.io][2])

---

# 二、第一步：实现无侵入任务追踪

在做调度前，先给原版任务附加自己的运行时状态。

## 2.1 不要使用全局 `Map<ChunkPos, TaskState>`

只用 `ChunkPos` 不够，因为：

* 不同维度可以有相同坐标；
* 同一区块的目标状态可能提升或降低；
* 原任务取消后可能创建新任务；
* 区块可能同时存在加载与生成路径；
* `ChunkPos` 对应的对象生命周期不一致。

推荐为 `ChunkGenerationTask` 或 `GenerationChunkHolder` 增加扩展字段：

```java
public interface SteadyGenerationExtension {
    SteadyTaskContext steady$getContext();
    void steady$setContext(SteadyTaskContext context);
}
```

Mixin 中：

```java
@Mixin(ChunkGenerationTask.class)
abstract class ChunkGenerationTaskMixin
        implements SteadyGenerationExtension {

    @Unique
    private SteadyTaskContext steady$context;

    @Override
    public SteadyTaskContext steady$getContext() {
        return this.steady$context;
    }

    @Override
    public void steady$setContext(SteadyTaskContext context) {
        this.steady$context = context;
    }
}
```

这里的 `SteadyTaskContext` 不要引用玩家实体、服务器或大型集合。它应尽量紧凑：

```java
final class SteadyTaskContext {
    final long taskId;
    final long packedChunkPos;
    final int dimensionId;

    volatile int targetStage;
    volatile int currentStage;
    volatile int stateFlags;

    volatile long demandVersion;
    volatile long enqueueNanos;
    volatile long stageStartNanos;

    volatile int nearestPlayerDistanceSq;
    volatile int demandCount;
}
```

建议使用整数 ID：

* `dimensionId` 由每个服务器运行实例分配；
* `stage` 映射到连续整数；
* `stateFlags` 使用位标志；
* 不在热路径保存 `ResourceLocation` 字符串。

## 2.2 任务 ID 必须区分生命周期

```java
private static final AtomicLong NEXT_TASK_ID = new AtomicLong();

long taskId = NEXT_TASK_ID.incrementAndGet();
```

诊断报告可以用：

```text
dimension + chunkPos + taskId
```

从而区分同一区块的不同生成尝试。

## 2.3 不要在每次事件中创建 Java Record

分析模式中每秒可能产生大量事件。以下写法容易产生过多对象：

```java
events.add(new StageEvent(...));
```

推荐每个工作线程拥有固定容量的原始类型环形缓冲：

```java
final class StageEventBuffer {
    final long[] taskIds;
    final long[] timestamps;
    final long[] chunkPositions;
    final int[] stages;
    final byte[] eventTypes;

    int writeIndex;
}
```

工作线程只执行数组写入。后台聚合器定期读取并生成：

* 阶段直方图；
* 排队时间；
* 执行时间；
* 最大值；
* 超时任务；
* 在途数量。

普通模式只保存聚合值；只有卡顿触发后才保留前后几秒的详细事件。

---

# 三、真正接管调度的位置

## 3.1 首选介入点：`ChunkMap.applyStep`

`ChunkMap.applyStep` 接收：

* `GenerationChunkHolder`
* `ChunkStep`
* 邻区块 `StaticCache2D`

并返回 `CompletableFuture<ChunkAccess>`。这是很理想的阶段准入位置，因为原版此时已经决定要执行什么步骤，并准备了对应的依赖上下文。([nekoyue.github.io][3])

第一版不要替换 `ChunkGenerationTask` 的全部运行逻辑，而是将原来的阶段调用包装成一个延迟操作：

```java
@FunctionalInterface
interface StageOperation {
    CompletableFuture<ChunkAccess> start();
}
```

概念代码：

```java
CompletableFuture<ChunkAccess> submitControlled(
        StageExecutionContext context,
        StageOperation originalOperation
) {
    CompletableFuture<ChunkAccess> exposedFuture =
            new CompletableFuture<>();

    ScheduledStageTask task = new ScheduledStageTask(
            context,
            originalOperation,
            exposedFuture
    );

    scheduler.enqueue(task);
    return exposedFuture;
}
```

调度器批准运行后：

```java
void startTask(ScheduledStageTask task) {
    PermitLease lease = permitManager.tryAcquire(task.resources());

    if (lease == null) {
        scheduler.requeue(task);
        return;
    }

    CompletableFuture<ChunkAccess> actualFuture;

    try {
        actualFuture = task.operation().start();
    } catch (Throwable throwable) {
        lease.close();
        task.exposedFuture().completeExceptionally(throwable);
        return;
    }

    actualFuture.whenComplete((chunk, throwable) -> {
        try {
            if (throwable != null) {
                task.exposedFuture().completeExceptionally(throwable);
            } else {
                task.exposedFuture().complete(chunk);
            }
        } finally {
            lease.close();
            scheduler.signalWorkAvailable();
        }
    });
}
```

## 3.2 绝对不要让工作线程阻塞等待 permit

错误实现：

```java
stageSemaphore.acquire();
try {
    return originalOperation.get();
} finally {
    stageSemaphore.release();
}
```

这会导致：

* 所有工作线程都阻塞在 Semaphore；
* 真正能解除依赖的任务没有线程执行；
* 嵌套 Future 容易形成线程饥饿；
* 表面看起来像区块死锁。

正确模型是：

```text
拿不到 permit
→ 任务留在 SteadyChunks 队列
→ 不占用任何工作线程
```

也就是异步准入，而不是在线程内部等待。

## 3.3 第一版不应立刻替换 `runGenerationTasks`

`runGenerationTasks` 可以用于：

* 通知调度器当前原版准备推进任务；
* 收集任务批次；
* 在 Tick 末执行调度；
* 记录原版队列深度。

但如果直接覆盖它，就需要重新实现大量任务 claim、释放和依赖处理逻辑。原版 `ChunkGenerationTask` 自身还维护 `scheduledStatus`、`scheduledLayer`、`needsGeneration`、取消标记和中心 Holder 等状态，过早替换非常容易产生永久未完成 Future。([nekoyue.github.io][2])

推荐演进顺序：

```text
第一版：
包装 applyStep

第二版：
控制 runUntilWait 的恢复时机

第三版：
数据证明必要后，才接管 pendingGenerationTasks
```

---

# 四、调度任务的数据模型

推荐：

```java
final class ScheduledStageTask {
    final long taskId;
    final StageExecutionContext context;
    final StageOperation operation;
    final CompletableFuture<ChunkAccess> exposedFuture;

    volatile TaskState state;
    volatile long priorityVersion;
    volatile long enqueuedNanos;
}
```

其中：

```java
record StageExecutionContext(
        int dimensionId,
        long packedChunkPos,
        StageClass stageClass,
        int targetStatus,
        int currentStatus,
        int dependencyRadius,
        boolean generatedChunk,
        boolean requiredForDependency
) {}
```

## 4.1 阶段不要直接以 `ChunkStatus` 为资源单位

建议再抽象一层：

```java
enum StageClass {
    LOAD,
    STRUCTURE_PLANNING,
    STRUCTURE_REFERENCES,
    BIOMES,
    NOISE,
    SURFACE,
    CARVERS,
    FEATURES,
    LIGHT,
    FINALIZATION,
    IO_READ,
    IO_WRITE
}
```

原因是：

* 未来 FastNoise 或 Bye-Pregen 可能改变某阶段内部实现；
* 不同 `ChunkStep` 可能使用相同资源；
* 某些阶段同时需要多种资源；
* 资源调节不应该依赖映射名。

## 4.2 资源应支持多重限制

例如 `FEATURES` 任务同时消耗：

```text
WORLDGEN_CPU
FEATURES_WRITE
DIMENSION_OVERWORLD
```

结构规划消耗：

```text
WORLDGEN_CPU
STRUCTURE_PLANNING
DIMENSION_OVERWORLD
```

可表示为位图：

```java
final class ResourceMask {
    long lowBits;
    long highBits;
}
```

或者固定整数数组：

```java
int[] resourceIds;
```

资源获取顺序必须固定，例如按 `resourceId` 升序，否则多个任务同时需要多资源时容易产生锁顺序问题。

FlowSched 的公开定位就是一个可对多个资源进行低开销加锁的优先任务调度器，且 C2ME 的 1.21.1 回移仓库包含 FlowSched 子模块。它可以作为数据结构与调度算法的研究对象，但仍要先完成许可证和 NeoForge 适配审计。([GitHub][4])

---

# 五、工作线程池怎么设计

## 5.1 固定最大线程池，动态改变 permit

不要每隔几秒销毁、创建线程来调整并发。

建议：

```java
int cpu = Runtime.getRuntime().availableProcessors();

int maxWorkers = integratedServer
        ? Math.max(1, cpu - 2)
        : Math.max(1, cpu - 1);
```

这只是线程池最大容量，不代表始终允许这么多任务执行。真正的活跃度由全局和阶段 permit 控制。

```java
ThreadPoolExecutor worldgenExecutor =
        new ThreadPoolExecutor(
                maxWorkers,
                maxWorkers,
                0L,
                TimeUnit.MILLISECONDS,
                new LinkedBlockingQueue<>(),
                threadFactory
        );
```

更严格的实现应避免无界 `LinkedBlockingQueue`，因为实际任务应留在自己的优先队列里，只有获得 permit 的任务才提交执行器：

```java
new ThreadPoolExecutor(
    maxWorkers,
    maxWorkers,
    0L,
    TimeUnit.MILLISECONDS,
    new ArrayBlockingQueue<>(maxWorkers * 2),
    threadFactory,
    new ThreadPoolExecutor.AbortPolicy()
);
```

理论上执行器队列几乎不会积压，因为调度器应该只提交可以马上运行的任务。

## 5.2 不建议使用虚拟线程

世界生成主要是 CPU 密集型工作，虚拟线程不会增加物理计算资源，反而可能掩盖并发数量并加剧在途任务膨胀。

## 5.3 不要依赖线程优先级解决渲染饥饿

可以把世界生成线程设置为较低优先级作为辅助，但核心治理必须来自：

* 更少活跃工作线程；
* 阶段 permit；
* 背压；
* 主线程完成预算；
* 客户端反馈。

---

# 六、优先级系统的具体实现

不建议用一个复杂浮点公式，每次玩家移动都重排所有任务。

推荐组合：

```text
一级：紧迫等级 Bucket
二级：当前阶段
三级：距离
四级：等待年龄
```

例如：

```java
enum Urgency {
    VISIBLE_GAP,
    NEAR_PLAYER,
    DIRECTIONAL_FRONT,
    NORMAL,
    BACKGROUND,
    EXPIRED
}
```

## 6.1 完成优先

可以为阶段赋予进度值：

```java
static int progressScore(StageClass stage) {
    return switch (stage) {
        case FINALIZATION -> 900;
        case LIGHT -> 800;
        case FEATURES -> 700;
        case CARVERS -> 600;
        case SURFACE -> 500;
        case NOISE -> 400;
        case BIOMES -> 300;
        case STRUCTURE_REFERENCES -> 200;
        case STRUCTURE_PLANNING -> 100;
        default -> 0;
    };
}
```

但不要让后期阶段无条件压倒一切。一个非常远且已经失去需求的 LIGHT 任务，不应永远压制玩家眼前的 NOISE。

概念评分：

```java
long priority =
      urgencyWeight
    + progressWeight
    + distanceWeight
    + ageWeight
    + dependencyWeight
    + fairnessWeight;
```

建议使用 `long`，避免浮点和比较不稳定。

## 6.2 不要每 Tick 重建整个优先堆

玩家移动会不断改变距离。

两种可行方式：

### 方式 A：分桶队列

```text
0～2 区块
3～5 区块
6～9 区块
10～视距
远处
```

玩家移动一个区块时，只更新受影响任务。

### 方式 B：惰性优先级更新

任务记录：

```java
long priorityVersion;
long cachedPriority;
```

取出任务时发现版本过期：

```java
if (task.priorityVersion != demandTracker.version()) {
    task.cachedPriority = recompute(task);
    task.priorityVersion = demandTracker.version();
    queue.add(task);
    continue;
}
```

这避免一次更新数千个任务。

## 6.3 多玩家公平性

推荐维度级与玩家级两层 Deficit Round Robin：

```text
维度队列
  └─ 玩家需求队列
       └─ 区块阶段任务
```

每个玩家每轮获得一定“预算积分”：

```java
player.deficit += player.quantum;
```

运行任务时扣除估算成本：

```java
player.deficit -= task.estimatedCost();
```

结构和 FEATURES 的成本高于 BIOMES。

一个任务被多个玩家需要时，不应重复运行。可以选择：

* 归属最近玩家；
* 或归属当前 deficit 最充足的需求玩家；
* 任务完成后同时满足所有需求者。

---

# 七、背压如何实现

背压不能只看线程数量。

建议至少监控：

```text
总等待任务
总在途任务
各阶段在途任务
ProtoChunk 数量
等待邻区块依赖数量
FULL 队列
区块发送队列
堆占用
```

定义压力等级：

```java
enum PressureLevel {
    NORMAL,
    ELEVATED,
    HIGH,
    EMERGENCY
}
```

响应示例：

### NORMAL

允许：

* 近处任务；
* 正常探索任务；
* 少量方向预测。

### ELEVATED

停止：

* 远处预取；
* 新的低优先级早期阶段。

继续：

* 近处区块；
* 已接近完成的任务；
* 依赖解锁任务。

### HIGH

只启动：

* 玩家可视缺口；
* 依赖解锁；
* LIGHT / FINALIZATION；
* 必须完成的 I/O。

### EMERGENCY

* 不再启动新的 STRUCTURE_STARTS、NOISE、FEATURES；
* 只排空关键完成回调；
* 暂时降低发送；
* 等待 GC、MSPT 或客户端帧时间恢复。

## 7.1 依赖解锁任务必须有保留资源

假设：

* A 正在等待 B；
* B 属于低优先级远区块；
* 调度器不再允许远区块任务；
* A 永远无法完成。

因此任务必须有：

```java
boolean requiredForDependency;
```

并预留一个或多个 dependency permits：

```text
普通 permit：3
依赖解锁 permit：1
```

普通任务不能占用最后一个依赖 permit。

---

# 八、软取消的实现

`ChunkGenerationTask` 本身存在 `markedForCancellation` 和公开的 `markForCancellation`，说明原版已经考虑任务取消语义，但 SteadyChunks 不应将其理解为“可以随时终止线程”。([nekoyue.github.io][2])

推荐状态机：

```java
enum TaskState {
    QUEUED,
    ADMITTED,
    RUNNING,
    COMPLETING,
    COMPLETED,
    CANCEL_REQUESTED,
    CANCELLED,
    FAILED
}
```

允许取消：

```text
QUEUED
```

允许请求取消但不中断：

```text
ADMITTED
RUNNING
```

处理：

```java
if (task.state == QUEUED) {
    task.state = CANCELLED;
    task.exposedFuture.completeExceptionally(CANCELLATION);
}
```

对于正在执行：

```java
task.state = CANCEL_REQUESTED;
```

阶段完成后：

```java
if (task.state == CANCEL_REQUESTED
        && !demandTracker.isStillRequired(task)
        && !dependencyTracker.isRequired(task)) {
    preventNextStage(task);
}
```

不要执行：

```java
actualFuture.cancel(true);
```

也不要 `Thread.interrupt()` 世界生成线程。大多数生成代码没有设计中断恢复语义。

## 8.1 FEATURES 开始后默认不可取消

FEATURES 中可能已经：

* 放置一半结构；
* 创建方块实体；
* 写邻区块；
* 添加后处理；
* 消耗随机数；
* 修改结构引用。

因此软取消只能阻止它进入 FEATURES，不能在中途终止。

---

# 九、自适应资源控制器

建议固定工作线程池，动态调整：

```text
global worldgen permit
structure permit
noise permit
features permit
light permit
FULL commit budget
send budget
```

## 9.1 初期使用 AIMD

示例：

```java
void update(PressureSnapshot p) {
    if (p.emergency()) {
        permits.multiply(0.5);
        enterEmergencyMode();
        cooldown = 100;
        return;
    }

    if (p.overloaded()) {
        permits.multiply(0.75);
        cooldown = 40;
        return;
    }

    if (cooldown > 0) {
        cooldown--;
        return;
    }

    if (p.healthyForLongEnough()) {
        permits.incrementOneStage();
        cooldown = 100;
    }
}
```

不要同时提高所有阶段。建议轮流提高：

```text
NOISE → STRUCTURE → LIGHT → FEATURES
```

提升后观察一个窗口，再决定下一步。

## 9.2 压力快照

```java
record PressureSnapshot(
    double processCpu,
    double p95Mspt,
    double p99FrameTime,
    double heapRatio,
    long recentGcPauseMillis,
    int visibleChunkGaps,
    int inflightTasks,
    int protoChunkCount,
    int fullQueueSize,
    int sendQueueSize,
    int clientBuildQueueSize
) {}
```

客户端不安装模组时：

```text
p99FrameTime = unavailable
clientBuildQueueSize = unavailable
```

控制器自动退化为服务端模式。

## 9.3 客户端反馈频率

不应每帧发包。

建议每 1～2 秒发送聚合值：

```text
P95 frame time
P99 frame time
最大 frame time
收到的区块数
Section 编译队列
最近可见缺块数
```

服务端只将其视为提示，不信任客户端提供的绝对值，不允许它改变游戏逻辑或获得额外权限。

---

# 十、FULL 整合的实现边界

这里非常容易做错。

`ChunkMap` 在生成后还包含 `prepareAccessibleChunk`、`prepareTickingChunk` 和 `onChunkReadyToSend` 等路径。([nekoyue.github.io][3])

不建议第一版直接延迟任何 `CompletableFuture<LevelChunk>` 的完成，因为其他原版任务可能等待它。

更安全的顺序是：

## 10.1 先限制进入最终阶段的任务

通过 LIGHT / FINALIZATION permit，让达到 FULL 的结果自然分散。

```text
不要一次让 8 个区块进入 LIGHT
→ 就不会一次产生 8 个 FULL
```

## 10.2 再平滑区块发送

区块已经完成并处于合法状态后，可以控制：

* 何时构建发送任务；
* 每玩家每 Tick 发送多少区块；
* 每玩家每 Tick 发送多少字节；
* 哪些近处区块优先。

C2ME 的新版本后来专门加入了平滑区块发送速率，并将改善帧时间稳定性列为目的，这说明“完成吞吐量”和“客户端可承受的送达节奏”需要分开治理。([GitHub][5])

## 10.3 最后才考虑主线程 commit 队列

必须通过 Phase 2 确认哪些操作：

* 仅影响区块进入玩家追踪；
* 可以延迟；
* 不会解除生成依赖；
* 不会影响 Ticket 状态。

如果无法证明，就不要延迟。

一个安全队列必须支持“关键任务旁路”：

```java
if (commit.isDependencyCritical()) {
    mainThreadExecutor.execute(commit);
} else {
    deferredCommitQueue.add(commit);
}
```

每 Tick：

```java
long deadline = System.nanoTime() + commitBudgetNanos;

while (System.nanoTime() < deadline) {
    CommitTask task = deferredCommitQueue.poll();
    if (task == null) break.nanoTime() < deadline) {
    Commit;

    task.run();
}
```

按数量限制通常不够，因为不同区块 commit 成本差异很大。最终应使用时间预算加最大数量双限制。

---

# 十一、结构规划优化的实现

结构规划主要可以从：

```text
ChunkStatusTasks.generateStructureStarts
→ ChunkGenerator.createStructures
→ ChunkGenerator.tryGenerateStructure
→ Structure.findGenerationPoint
→ JigsawPlacement.addPieces
```

进行分层统计。`ChunkGenerator` 明确包含 `createStructures`、`tryGenerateStructure` 和 `createReferences`；Jigsaw 规划由 `JigsawPlacement.addPiece([nekoyue.github.io][6])ew5turn679335view0turn679335view1

## 11.1 第一阶段只做计时归因

使用线程上下文：

```java
final class WorldgenTraceContext {
    long taskId;
    ResourceLocation structureId;
    ResourceLocation templatePoolId;
    int pieceCount;
    int collisionChecks;
}
```

在 `tryGenerateStructure` 前设置结构 ID，退出时清除。

异步执行时不要依靠普通全局变量，应使用：

```java
private static final ThreadLocal<WorldgenTraceContext> TRACE =
        ThreadLocal.withInitial(WorldgenTraceContext::new);
```

每个入口必须：

```java
WorldgenTraceContext ctx = TRACE.get();
TraceSnapshot previous = ctx.push(...);

try {
    return originalCall();
} finally {
    ctx.pop(previous);
}
```

防止嵌套结构调用污染上下文。

## 11.2 候选快速否决缓存

可缓存的通常是纯输入判断，例如：

```text
某 StructureSet 在某 ChunkPos 是否为 placement 候选
某结构允许的 biome holder set
固定模板池元数据
模板尺寸
模板连接点列表
```

键示例：

```java
record PlacementKey(
    int dimensionId,
    int structureSetRawId,
    long packedChunkPos,
    long datapackGeneration
) {}
```

值：

```java
byte result; // UNKNOWN / NO / YES
```

不要缓存整个 `StructureStart`，也不要跨世界共享：

* Piece 列表；
* 随机选择结果；
* Loot；
* 最终 NBT；
* 依赖当前世界高度的结果。

## 11.3 Jigsaw 模板元数据缓存

对 `StructureTemplate` 可以预计算：

```java
final class TemplateMetadata {
    int sizeX;
    int sizeY;
    int sizeZ;

    ConnectorInfo[] jigsawConnectors;
    BoundingBox localBounds;

    int[] staticBlockIndices;
    int[] blockEntityIndices;
}
```

这个缓存只保存静态模板元数据，不保存经过 Rotation、Mirror、Processor、随机数和世界查询后的最终方块。

`StructureTemplate` 与 `StructureProcessor` 分别承担模板放置和处([nekoyue.github.io][7])iteturn679335view3turn679335view4

## 11.4 VoxelShape 优化要谨慎

Jigsaw 放置使用 VoxelShape 处理已占用空间。可优化方向：

* 先用 `BoundingBox` 快速排除；
* 只有包围盒相交时才执行 VoxelShape 运算；
* 缓存不可变局部形状；
* 减少等价形状反复构造；
* 避免在每个候选上重新创建相同边界对象。

但不建议第一版自行实现新的几何碰撞算法。应先证明 VoxelShape 运算确实位于目标结构的 P99 热点。

---

# 十二、结构引用优化

`ChunkGenerator.createReferences` 和内部的([nekoyue.github.io][8])结构起点与区块关系建立引用。citeturn679335view0

可以构建生成批次内的空间索引：

```java
final class StructureStartIndex {
    Long2ObjectMap<List<IndexedStart>> startsByRegion;
}
```

每个起点保存：

```java
record IndexedStart(
    int structureRawId,
    long startChunkPos,
    int minChunkX,
    int minChunkZ,
    int maxChunkX,
    int maxChunkZ
) {}
```

建立引用时：

```text
先按区域查候选起点
→ 再检查 Chunk 包围盒
→ 最后调用原版引用写入
```

注意：

* 索引只是候选加速器；
* 最终是否写入引用仍使用原版条件；
* 索引生命周期不能超过对应结构数据；
* 区块卸载和任务取消必须清除；
* 大型结构跨越多个 Region 时要登记到所有覆盖区域，或使用中心加范围查询。

---

# 十三、FEATURES 和结构实际放置

`ChunkStatusTasks.generateFeatures` 最终会进入 `ChunkGenerator.applyBiomeDecor([nekoyue.github.io][6])iteturn233583view6turn679335view0

必须继续拆分：

```text
applyBiomeDecoration
├─ 每个 generation step
├─ 每个结构
│  └─ 每个 StructurePiece
├─ 每个 PlacedFeature
└─ 每个 StructureProcessor
```

## 13.1 结构 Piece 计时

在 `StructureStart.placeInChunk` 或对应 Piece 放置入口包装：

```java
long start = System.nanoTime();
try {
    original.placeInChunk(...);
} finally {
    metrics.recordStructurePiece(
        structureId,
        piece.getClass(),
        System.nanoTime() - start
    );
}
```

`StructureStart` 是结构 Piece 容器并承([nekoyue.github.io][9])“结构实际写入”的关键位置。citeturn679335view2

## 13.2 Processor 能力分级

不能自动假定 Processor 是纯函数。

建议公开接口：

```java
public interface ProcessorTraitsProvider {
    ProcessorTraits steady$getTraits();
}
```

```java
record ProcessorTraits(
    boolean readsWorld,
    boolean writesWorld,
    boolean usesRandom,
    boolean positionDependent,
    boolean cacheable
) {}
```

默认：

```java
UNKNOWN = new ProcessorTraits(
    true,
    true,
    true,
    true,
    false
);
```

只有原版已审计 Processor 或适配模组明确注册后，才提升为可缓存。

## 13.3 可缓存 Processor 结果

对于纯静态 Processor，可以缓存：

```text
模板 ID
palette ID
原始 block info 索引
rotation
mirror
processor list generation
```

输出必须只包含与世界无关的中间结果，而不是最终 BlockPos。

例如缓存：

```java
record StaticProcessedBlock(
    short localX,
    short localY,
    short localZ,
    int blockStateRawId,
    int sourceNbtIndex
) {}
```

放置时再进行：

* 世界坐标转换；
* 世界高度检查；
* 邻区块检查；
* 动态 Processor；
* Loot 与方块实体 NBT。

## 13.4 不要尝试中断一个大型 StructurePiece

第一版通过：

* `FEATURES` permit 为 1 或 2；
* 结构成本预测；
* 避免两个超大型结构同时执行；

来减少尖峰。

Piece 级切分只有满足以下条件后才能实验：

* Piece 相互独立；
* 随机源可按 Piece 确定性派生；
* 方块覆盖顺序不影响结果；
* Processor 不依赖之前 Piece 的写入；
* 后处理顺序可证明等价。

否则即使性能更好，也可能改变世界生成结果。

---

# 十四、FastNoise 与 Bye-Pregen 兼容的具体方法

Bye-Pregen 的源码目录已经明确分为 Feature、PaletteContainer、PostProcess、GC-free、worldgen、light、compat 和 MixinGate 等模块，因此 SteadyChunks 不能([GitHub][10])要按目标类和方法维护所有权。citeturn403996view1

## 14.1 建立静态所有权表

```java
enum OptimizationOwner {
    VANILLA,
    STEADY_CHUNKS,
    FAST_NOISE,
    BYE_PREGEN
}
```

```java
record OptimizationOwnership(
    OptimizationOwner noiseFill,
    OptimizationOwner biomeFill,
    OptimizationOwner surfaceBuild,
    OptimizationOwner paletteWrite,
    OptimizationOwner postProcess,
    OptimizationOwner serializer,
    OptimizationOwner lightEngine,
    OptimizationOwner scheduler,
    OptimizationOwner chunkSend
) {}
```

启动日志：

```text
[SteadyChunks] Optimization ownership:
  scheduler               = STEADY_CHUNKS
  noise_fill              = FAST_NOISE
  palette_write           = BYE_PREGEN
  serializer              = BYE_PREGEN
  light_algorithm         = BYE_PREGEN
  light_scheduling        = STEADY_CHUNKS
  structure_planning      = STEADY_CHUNKS
```

## 14.2 MixinConfigPlugin 中关闭冲突 Mixin

```java
@Override
public boolean shouldApplyMixin(
        String targetClassName,
        String mixinClassName
) {
    if (mixinClassName.contains(".noise.")
            && compatibility.fastNoiseOwnsNoise()) {
        return false;
    }

    if (mixinClassName.contains(".serializer.")
            && compatibility.byePregenOwnsSerializer()) {
        return false;
    }

    return true;
}
```

不要在 Mixin 已经应用后才尝试运行时关闭。

## 14.3 SteadyChunks 应包装阶段，不包装第三方内部实现

正确：

```text
SteadyChunks 准入
→ 调用 ChunkStatus 原阶段
→ FastNoise 或 Bye-Pregen 的 Mixin 自然接管内部实现
→ Future 完成
→ SteadyChunks 释放 permit
```

错误：

```text
SteadyChunks 复制一份原版 NOISE
→ FastNoise 无法介入
→ 两边分别维护同一算法
```

## 14.4 版本适配器

```java
interface CompatAdapter {
    boolean supports(String version);
    OwnershipResult detectOwnership();
    List<String> validateMixins();
}
```

每个测试过的版本有独立适配器。遇到未知版本：

```text
只启用调度、遥测和发送平滑
关闭与第三方同目标方法的内部优化
```

---

# 十五、光照的实现建议

`ChunkStatusTasks` 将光照([nekoyue.github.io][6]) `light` 两个任务。citeturn233583view6

初期不要实现新的光照算法，而是只管理：

```text
进入 initializeLight 的数量
进入 light 的数量
光照完成回调数量
LIGHT → FULL 的完成节奏
```

若 Bye-Pregen 接管光照算法：

```text
Bye-Pregen：如何算光
SteadyChunks：什么时候允许算、同时算多少、什么时候放行结果
```

要为光照依赖保留 permit，避免：

```text
区块 A 等待 B 的边界光照
B 因距离较远得不到 permit
A 永久等待
```

可设置：

```toml
light_permits = 2
light_dependency_reserve = 1
```

---

# 十六、I/O 的实现建议

I/O 不应与 CPU 世界生成共用同一个池。

```text
Worldgen CPU Pool
Chunk Read Pool
Chunk Compression Pool
Chunk Write Pool
Main Thread
```

但线程池也不能无限拆分，否则线程数量和调度开销继续上升。

最低限度：

```java
ExecutorService worldgenPool;
ExecutorService ioPool;
```

进一步可拆：

```java
ExecutorService compressionPool;
```

## 16.1 保存背压

```java
if (pendingWrites > highWatermark) {
    scheduler.disableBackgroundGeneration();
}

if (pendingWrites > emergencyWatermark) {
    scheduler.reduceWorldgenPermits();
    ioGovernor.raiseWritePriority();
}
```

不能一直优先读取而让写入饥饿，否则：

* 旧区块不能卸载；
* 内存持续增加；
* 停服时积压巨大。

写任务应有最大等待时间，超过后自动提升优先级。

## 16.2 与 Bye-Pregen 序列化兼容

SteadyChunks 只应控制：

* 任务何时提交；
* 同时压缩多少；
* 队列上限；
* 停服排空；
* RegionFile 顺序。

序列化内部对象分配由 Bye-Pregen 负责。

---

# 十七、异常与生命周期必须从第一天实现

所有任务必须统一通过：

```java
void completeTask(
    ScheduledStageTask task,
    Object result,
    Throwable throwable
)
```

这里集中处理：

* permit 释放；
* 任务状态；
* 指标；
* Future 完成；
* Holder 引用释放；
* 调度器唤醒。

不要把释放逻辑分散到每个 Mixin。

## 17.1 维度卸载

```java
void unloadDimension(int dimensionId) {
    scheduler.rejectNewTasks(dimensionId);
    scheduler.cancelQueuedTasks(dimensionId);
    caches.invalidateDimension(dimensionId);
    ioQueues.flushDimension(dimensionId);
    scheduler.awaitCriticalTasks(dimensionId);
}
```

正在执行的 FEATURES 不强制中断，但服务器关闭流程要等待或走原版失败逻辑。

## 17.2 数据包重载

每次 reload 增加 generation：

```java
long datapackGeneration++;
```

所有缓存键包含这个 generation，旧缓存即使未马上清理也不会再命中。

reload 完成后异步回收旧 generation 数据。

## 17.3 Watchdog

定期扫描：

```text
QUEUED 太久
RUNNING 太久
Future 无消费者
持有 permit 太久
依赖链无进展
维度已卸载但任务仍存在
```

只报告，不自动杀线程。

---

# 十八、建议按这些开发 PR 推进

## PR 1：只做追踪

实现：

* `ChunkGenerationTask` 扩展字段；
* `applyStep` 开始/结束计时；
* 各 ChunkStatus 统计；
* ProtoChunk 和在途任务计数；
* JSON/Markdown 导出。

不要改变调度。

## PR 2：固定阶段 Gate

只限制：

```text
NOISE
FEATURES
LIGHT
```

使用代理 Future，不阻塞工作线程。

验收：

* 无死锁；
* permit 无泄漏；
* 能看到并发下降；
* 生成结果不变。

## PR 3：完成优先和背压

加入：

* 距离分桶；
* 阶段进度；
* 等待年龄；
* ProtoChunk 高水位；
* 依赖保留 permit。

## PR 4：发送平滑

先不延迟 FULL Future，只限制每玩家区块发送数量和字节数。

重点测试：

* 高速直线飞行；
* 高速转向；
* 近处缺块；
* 客户端帧时间。

## PR 5：自适应 Governor

先使用：

* MSPT；
* CPU；
* 堆压力；
* 队列深度。

客户端反馈作为后续提交加入，避免一次引入太多变量。

## PR 6：结构深度分析

添加：

* `createStructures`
* `tryGenerateStructure`
* Jigsaw Piece 数；
* `applyBiomeDecoration`
* Structure Piece；
* PlacedFeature；
* Processor

的归因。

## PR 7：第一个结构优化

只选择分析报告中成本最高、且能够证明正确性的一个点，例如：

* 模板连接点元数据缓存；
* StructureSet 候选缓存；
* 包围盒快速排除；
* 重复模板查找消除。

不要一次实现所有结构优化。

---

# 十九、开发中最容易犯的错误

最危险的几个错误是：

1. **拿不到 permit 时阻塞工作线程。**
2. **把 `CompletableFuture.cancel(true)` 当成安全取消。**
3. **只限制线程数，不限制在途 Future 和 ProtoChunk。**
4. **任意延迟 FULL Future，导致依赖链等待。**
5. **把所有后期阶段都设置为最高优先级，造成近处新区块无法启动。**
6. **缓存随机数相关结果，改变世界生成结果。**
7. **假定所有 StructureProcessor 都是纯函数。**
8. **在 FastNoise 或 Bye-Pregen 已修改的方法中复制一套实现。**
9. **在热路径创建大量事件对象用于诊断。**
10. **只测平均 CPS，不测 P99 帧时间、MSPT 和长帧次数。**
11. **只在专用服务器测试，不测集成服务器 CPU 争抢。**
12. **一次重写调度、光照、I/O 和结构，最终无法判断哪个模块导致回归。**

---

# 二十、最关键的实现顺序

从工程角度，真正的主线应是：

```text
保留原版依赖图
        ↓
包装 applyStep
        ↓
实现非阻塞阶段准入
        ↓
限制在途任务和 ProtoChunk
        ↓
完成优先与依赖保底
        ↓
自适应调节 permit
        ↓
平滑 LIGHT / FULL / 发送
        ↓
定位结构长尾
        ↓
只优化已证明的结构热点
        ↓
最后才考虑替换更多原版子系统
```

其中最有决定性的设计不是某一个 Mixin，而是：

> **所有调度限制必须发生在任务占用工作线程之前；所有取消必须发生在阶段写入开始之前；所有缓存必须明确区分静态元数据与随机、位置、世界相关结果。**

只要这三个原则守住，SteadyChunks 就可以逐步增加并行和优化强度；如果一开始就跨过这些边界，即跑分很高，也很容易重现 C2ME 类项目最难解决的稳定性与兼容性问题。

[1]: https://docs.neoforged.net/primer/docs/1.21/ "1.20.6 -> 1.21 | NeoForged docs"
[2]: https://nekoyue.github.io/ForgeJavaDocs-NG/javadoc/1.21.x-neoforge/net/minecraft/server/level/ChunkGenerationTask.html "ChunkGenerationTask (neoforge 1.21.0-21.0.30-beta)"
[3]: https://nekoyue.github.io/ForgeJavaDocs-NG/javadoc/1.21.x-neoforge/net/minecraft/server/level/ChunkMap.html "ChunkMap (neoforge 1.21.0-21.0.30-beta)"
[4]: https://github.com/RelativityMC/C2ME-fabric/tree/backports/1.21.1 "GitHub - RelativityMC/C2ME-fabric at backports/1.21.1 · GitHub"
[5]: https://github.com/RelativityMC/C2ME-fabric/releases?utm_source=chatgpt.com "Releases · RelativityMC/C2ME-fabric"
[6]: https://nekoyue.github.io/ForgeJavaDocs-NG/javadoc/1.21.x-neoforge/net/minecraft/world/level/chunk/status/ChunkStatusTasks.html "ChunkStatusTasks (neoforge 1.21.0-21.0.30-beta)"
[7]: https://nekoyue.github.io/ForgeJavaDocs-NG/javadoc/1.21.x-neoforge/net/minecraft/world/level/levelgen/structure/templatesystem/StructureTemplate.html "StructureTemplate (neoforge 1.21.0-21.0.30-beta)"
[8]: https://nekoyue.github.io/ForgeJavaDocs-NG/javadoc/1.21.x-neoforge/net/minecraft/world/level/chunk/ChunkGenerator.html "ChunkGenerator (neoforge 1.21.0-21.0.30-beta)"
[9]: https://nekoyue.github.io/ForgeJavaDocs-NG/javadoc/1.21.x-neoforge/net/minecraft/world/level/levelgen/structure/StructureStart.html "StructureStart (neoforge 1.21.0-21.0.30-beta)"
[10]: https://github.com/MoePus/Bye-Pregen/tree/master/src/main/java/com/moepus/byepregen "Bye-Pregen/src/main/java/com/moepus/byepregen at master · MoePus/Bye-Pregen · GitHub"
